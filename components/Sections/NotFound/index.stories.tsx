import NotFoundSection from './';
import type { Meta as MetaObj, StoryObj } from '@storybook/react';

type Story = StoryObj<typeof NotFoundSection>;
type Meta = MetaObj<typeof NotFoundSection>;

export const Default: Story = {};

export default { component: NotFoundSection } as Meta;
